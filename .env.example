# Postgres (Neon) connection string. Get this by attaching the Neon storage
# integration to the Vercel project (dashboard → Storage, or `vercel install
# neon`), then `vercel env pull .env`, or copy it from the Neon dashboard directly.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
